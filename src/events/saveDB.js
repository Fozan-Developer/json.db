const fs = require('fs');
const path = require('path');

async function save(argument) {
	// require('fs').writeFileSync('./base/stavka.json', JSON.stringify(stavka, null, '\t'));
};

module.exports = async function saveDB(time) {
	setInterval(async () => {

	}, time);
};